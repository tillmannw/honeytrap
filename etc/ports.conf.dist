# honeytrap configuration file
# Explicit configuration for some tcp ports
#
# the syntax is 'port = portnum,mode[,host,port]'
# e.g. 'port = 80,mirror'
#
# possible values for mode are:
# ignore:	ignore connection requests on this port
# normal:	always handle connections on this port in normal mode
# mirror:	always handle connections on this port in mirror mode
# proxy:	always handle connections on this port in proxy mod
#
# 'proxy' takes the arguments 'host,port' where port is optional
# e.g. 'port = 80,proxy,example.com:8000
# if no port argument is given, the attacked port is used.
#

#port = 21,normal
#port = 22,ignore
#port = 25,mirror
#port = 80,proxy,example.com:8000
